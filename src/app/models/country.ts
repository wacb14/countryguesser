export class Country {
  code: string;
  name: string;
  continent: string;
  constructor(code: string, name: string, continent: string) {
    this.code = code;
    this.name = name;
    this.continent = continent;
  }
}
